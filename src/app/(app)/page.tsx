// Home dashboard, per docs/design/ui-ux.md. The financial-summary snapshot
// and recent-activity list land once Stage 3 builds loads/expenses/summary
// (tasks 3.6-3.11) - this is real navigation now, not a placeholder, but
// its content grows as later tasks land.
export default function HomePage() {
  return (
    <div>
      <h1>Trucking OS</h1>
      <p>Dashboard content lands as Stage 3 tasks complete — see PROJECT_STATE.md.</p>
    </div>
  );
}
