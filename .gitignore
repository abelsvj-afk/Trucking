# dependencies
/node_modules

# Next.js build output
/.next/
/out/

# testing
/coverage
/playwright-report
/test-results
# e2e auth session state + shared test-fixture ids (real, if disposable,
# credentials/session tokens - never committed, see tests/e2e/fixtures)
/tests/e2e/.auth

# environment — never commit real values, see .env.example
.env
.env.local
.env.*.local

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript
*.tsbuildinfo
next-env.d.ts
