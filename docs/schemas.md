# Data Schemas

Phase 6 deliverable per `MASTER AI ENGINEERING & SYSTEM DEVELOPMENT WORKFLOW`. This is a contract: once implementation starts, code conforms to this, not the other way around. Covers the MVP entities from `docs/requirements.md`, built on the relational, `company_id`-scoped approach decided in `docs/architecture.md`.

## Global conventions (apply to every table below)

- **Primary key:** `id`, UUID, generated by the database.
- **Tenant scoping:** every table has a `company_id` (UUID, references `companies.id`, not null). Row-level security restricts every query to the caller's own `company_id` — this is the architectural decision from `docs/architecture.md`, applied concretely.
- **Timestamps:** every table has `created_at` and `updated_at` (both `timestamptz`, defaulting to now, `updated_at` maintained on write).
- **Soft delete:** every table has a nullable `deleted_at` (`timestamptz`). Deleting a record sets this instead of removing the row — per the workflow doc's "reversible actions beat irreversible actions" principle and the reliability requirement in `docs/requirements.md` that data must never be silently lost. Deleted records are excluded from normal reads but never physically erased. (No user asked for a stricter "no permanent deletes" rule in `docs/governance.md` — this is the ordinary engineering default for financial data, not an extra restriction.)
- **Money:** stored as integer cents (not floating point), to avoid rounding errors in financial figures.

## companies

The tenant table — one row per business using the system. Exists from day one even though there's exactly one row right now, per the multi-tenant-readiness decision in `docs/architecture.md`.

| Column | Type | Nullable | Notes |
|---|---|---|---|
| id | uuid | no | primary key |
| name | text | no | |
| created_at | timestamptz | no | |

## user_profiles

Extends Supabase Auth's built-in user record with app-specific fields. One row per person with access, per the single-admin access model in `docs/governance.md` (currently exactly one).

| Column | Type | Nullable | Notes |
|---|---|---|---|
| id | uuid | no | primary key, references `auth.users.id` |
| company_id | uuid | no | |
| role | text | no | `owner` today; the column exists so future roles (e.g. view-only collaborator, per `docs/governance.md`) don't require a schema change |
| display_name | text | yes | |
| created_at | timestamptz | no | |

## trucks

| Column | Type | Nullable | Notes |
|---|---|---|---|
| id | uuid | no | |
| company_id | uuid | no | |
| unit_number | text | no | operator-assigned identifier, e.g. "Truck #1" |
| vin | text | yes | |
| make | text | yes | |
| model | text | yes | |
| year | integer | yes | |
| status | text | no | `active` \| `maintenance` \| `inactive` |
| current_mileage | integer | yes | |
| created_at / updated_at / deleted_at | timestamptz | see global conventions | |

## trailers

| Column | Type | Nullable | Notes |
|---|---|---|---|
| id | uuid | no | |
| company_id | uuid | no | |
| unit_number | text | no | |
| vin | text | yes | |
| trailer_type | text | yes | |
| status | text | no | `active` \| `maintenance` \| `inactive` |
| created_at / updated_at / deleted_at | timestamptz | see global conventions | |

## drivers

| Column | Type | Nullable | Notes |
|---|---|---|---|
| id | uuid | no | |
| company_id | uuid | no | |
| name | text | no | |
| cdl_number | text | yes | |
| assigned_truck_id | uuid | yes | references `trucks.id` |
| status | text | no | `active` \| `inactive` |
| created_at / updated_at / deleted_at | timestamptz | see global conventions | |

## customers

| Column | Type | Nullable | Notes |
|---|---|---|---|
| id | uuid | no | |
| company_id | uuid | no | |
| name | text | no | |
| contact_email | text | yes | |
| contact_phone | text | yes | |
| notes | text | yes | |
| created_at / updated_at / deleted_at | timestamptz | see global conventions | |

## brokers

| Column | Type | Nullable | Notes |
|---|---|---|---|
| id | uuid | no | |
| company_id | uuid | no | |
| name | text | no | |
| mc_number | text | yes | |
| contact_email | text | yes | |
| contact_phone | text | yes | |
| notes | text | yes | |
| created_at / updated_at / deleted_at | timestamptz | see global conventions | |

## loads

| Column | Type | Nullable | Notes |
|---|---|---|---|
| id | uuid | no | |
| company_id | uuid | no | |
| truck_id | uuid | yes | references `trucks.id` |
| driver_id | uuid | yes | references `drivers.id` |
| broker_id | uuid | yes | references `brokers.id` |
| customer_id | uuid | yes | references `customers.id` |
| origin | text | no | |
| destination | text | no | |
| pickup_date | date | yes | |
| delivery_date | date | yes | |
| rate_cents | integer | yes | nullable so a load can be saved before the rate is confirmed |
| miles | integer | yes | |
| status | text | no | `draft` \| `confirmed` \| `completed` — `draft` supports the alternative "incomplete load" workflow in `docs/user-stories.md`; only `confirmed`/`completed` loads count toward the financial summary |
| notes | text | yes | |
| created_at / updated_at / deleted_at | timestamptz | see global conventions | |

## expenses

| Column | Type | Nullable | Notes |
|---|---|---|---|
| id | uuid | no | |
| company_id | uuid | no | |
| category | text | no | `insurance` \| `permits` \| `repairs` \| `other` |
| amount_cents | integer | no | |
| expense_date | date | no | |
| truck_id | uuid | yes | references `trucks.id` |
| load_id | uuid | yes | references `loads.id` |
| driver_id | uuid | yes | references `drivers.id` |
| description | text | yes | |
| created_at / updated_at / deleted_at | timestamptz | see global conventions | |

## fuel_purchases

| Column | Type | Nullable | Notes |
|---|---|---|---|
| id | uuid | no | |
| company_id | uuid | no | |
| truck_id | uuid | no | references `trucks.id` |
| location | text | yes | |
| gallons | numeric(8,3) | no | |
| price_per_gallon_cents | integer | no | |
| total_cost_cents | integer | no | |
| purchased_at | timestamptz | no | |
| created_at / updated_at / deleted_at | timestamptz | see global conventions | |

## maintenance_events

| Column | Type | Nullable | Notes |
|---|---|---|---|
| id | uuid | no | |
| company_id | uuid | no | |
| truck_id | uuid | no | references `trucks.id` |
| description | text | no | |
| cost_cents | integer | yes | |
| service_date | date | no | |
| mileage_at_service | integer | yes | |
| created_at / updated_at / deleted_at | timestamptz | see global conventions | |

## documents

| Column | Type | Nullable | Notes |
|---|---|---|---|
| id | uuid | no | |
| company_id | uuid | no | |
| related_entity_type | text | no | `truck` \| `trailer` \| `driver` \| `load` |
| related_entity_id | uuid | no | id of the related row |
| file_name | text | no | |
| storage_path | text | no | path in Supabase Storage |
| uploaded_at | timestamptz | no | |
| created_at | timestamptz | no | (documents are not edited in place, and are hard-deleted like any file — no `deleted_at`/`updated_at`) |

## industry_briefings

Added in Phase 10 (`docs/automation.md`) for the industry-intelligence engine — the first AI capability to move past design. Not part of the MVP; created only when that capability is actually built.

| Column | Type | Nullable | Notes |
|---|---|---|---|
| id | uuid | no | |
| company_id | uuid | no | kept per the global tenant-scoping convention for MVP simplicity — see `docs/automation.md` for why this may become a shared/global table later instead |
| summary | text | no | per the output contract in `docs/design/ai-architecture.md` |
| reasoning | text | no | |
| confidence | text | no | `high` \| `medium` \| `low` |
| based_on | jsonb | no | array of source references used |
| generated_at | timestamptz | no | when the scheduled run produced this |
| dismissed_at | timestamptz | yes | set when the owner dismisses it — this capability's equivalent of the standard soft-delete convention (`dismissed_at` reads better than `deleted_at` for a briefing, but behaves the same: excluded from the active list, never physically erased) |

## industry_briefing_runs

Added alongside `industry_briefings` (Stage 4, `docs/automation.md` task 4.7). A successful run is fully represented by the `industry_briefings` row it produces — but a **failed** run produces no briefing at all (per `docs/automation.md`'s Failure recovery: never a partial one), so there'd be nothing to check consecutive failures against without a separate log. This table is that log: one row per run attempt, success or failure, per `docs/automation.md`'s Audit trail requirement ("every run logs... a failed run logs the failure and why — never just silence").

| Column | Type | Nullable | Notes |
|---|---|---|---|
| id | uuid | no | |
| company_id | uuid | no | same tenant-scoping caveat as `industry_briefings` above |
| started_at | timestamptz | no | |
| finished_at | timestamptz | yes | null only if the process crashed mid-run without reaching its own error handler |
| status | text | no | `success` \| `failure` \| `insufficient_data` — the third value matters: per `docs/design/ai-architecture.md`'s output contract, a quiet news cycle with nothing worth surfacing is a normal, expected outcome, not a failure. Counting it toward the consecutive-failure escalation (`docs/automation.md`) would raise false alarms during ordinary quiet periods |
| error_message | text | yes | set only when `status = 'failure'`; server-side diagnostic detail only — never shown raw to the owner, per `CLAUDE.md`'s centralized error handling |
| briefing_id | uuid | yes | references `industry_briefings.id`; set only on `success` |
| created_at | timestamptz | no | |

No `deleted_at`/soft-delete — like `documents`, this is a deliberate deviation from the global convention: a run log is an immutable record of what happened, not a business record anyone edits or removes.

## ai_capability_settings

Added alongside `industry_briefings` (Stage 4, `docs/governance.md`'s per-capability revocation requirement). One row per company per capability; a capability with no row is treated as off (fails closed, per `CLAUDE.md`).

| Column | Type | Nullable | Notes |
|---|---|---|---|
| id | uuid | no | |
| company_id | uuid | no | |
| capability | text | no | e.g. `industry_intelligence`; one value per capability as each is built |
| enabled | boolean | no | defaults `false` — every capability ships off, per `docs/governance.md` |
| updated_at | timestamptz | no | |

No `created_at`/`deleted_at`: this is toggled in place (upserted on `(company_id, capability)`), not a record with its own lifecycle.

## companies — one addition

`docs/governance.md`'s **global** kill switch (distinct from any per-capability switch above) is a single column on `companies`, since it revokes *all* AI authority for that company at once regardless of what capabilities exist:

| Column | Type | Nullable | Notes |
|---|---|---|---|
| ai_globally_disabled | boolean | no | defaults `false`. When `true`, every capability's scheduler/trigger must refuse to run, full stop — checked before any per-capability switch |

## Financial summary

Not a stored table. It's computed on read from `loads` (status `confirmed`/`completed`) and `expenses`/`fuel_purchases`/`maintenance_events` within a date range — see `docs/api-contracts.md`'s `/financial-summary` endpoint. No dedicated table exists yet because there's no real performance reason for one (matches the "no caching yet" decision in `docs/architecture.md`) — revisit only if computing it live actually becomes slow.

---

*Column-level detail beyond what's needed to satisfy `docs/requirements.md`'s MVP scope is deliberately not included — e.g. no `documents` versioning, no `loads` rate-negotiation history. Add fields here, not silently in code, when a real need shows up.*
